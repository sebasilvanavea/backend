# Configuración del servidor - Puerto fijo para pruebas
server.port=8080

# Configuración de la base de datos PostgreSQL (desactivada para pruebas sin BD)
# spring.datasource.url=jdbc:postgresql://localhost:5432/railway_demo
# spring.datasource.username=postgres
# spring.datasource.password=password
# spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA/Hibernate (desactivada para pruebas sin BD)
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.properties.hibernate.format_sql=true

# Configuración de logging
logging.level.org.springframework.web=INFO
logging.level.com.example.railwaydemo=DEBUG

# Configuración de la aplicación
spring.application.name=railway-demo 